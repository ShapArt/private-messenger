# Infra (placeholder)

Planned docker-compose: Postgres, Redis, API (FastAPI), MinIO/S3 for presigned uploads, coturn for TURN relay. Add Makefile/scripts to generate TURN credentials via `TURN_SECRET`.
