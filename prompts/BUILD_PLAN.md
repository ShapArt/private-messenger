# BUILD PLAN — Private Messenger

## 🎯 Цели
- Сигнальный сервер + API для E2EE чата с WebRTC медиа-каналами.
- Минимальный клиент (web/PWA) для обмена текстом и файлами.

## 🔧 Стек и библиотеки
- FastAPI, uvicorn, WebSockets.
- WebRTC: aiortc для серверного сигнала + STUN/TURN (coturn).
- Crypto: libsodium/pyNaCl (X25519, ed25519), HKDF.
- DB: PostgreSQL + Alembic; S3 совместимый storage для вложений.
- pre-commit: gitleaks, black, isort, ruff, eol/trailing-whitespace.

## 🛡️ Риски и решения
- Утечки ключей: хранить только у клиента, на сервере — лишь публичные ключи/запросы. gitleaks обязательный.
- Replay/mitm: подписи на сообщения, проверка сессий, nonces, ограничения TTL.
- Стабильность WebRTC: fallback на TURN, троттлинг битрейта, контроль размера файлов.
- GDPR/retention: политики удаления, TTL на комнаты и вложения.

## 🧭 MVP-итерации
1) Auth stub (по email/otp), выдача пары ключей, handshake (public keys exchange).
2) WebSocket канал + хранение сообщений (зашифрованных), ack/receipt.
3) WebRTC сигналинг (offer/answer/ice), загрузка/скачивание вложений через S3 pre-signed.
4) Клиент SPA: basic chat + отправка файла, демо запись GIF.
5) CI lint/test (continue-on-error), docker-compose для dev.

## 📦 Definition of Done
- `.env.example`, `.gitignore`, pre-commit установлен, gitleaks чист.
- Миграции Alembic работают, health-check `/healthz` и `/readyz`.
- Документация: README (архитектура, конфиг, схемы потоков), demo steps.
- E2E тест: создание сессии, обмен сообщением, отправка файла.

## 🛰️ Демо сценарии
- QR/PIN обмен: два клиента подключаются, сканируют код, начинают чат.
- Отправка файла с подтверждением: загружаем, получатель принимает, таймер удаления тикает.
- Пересоздание сессии после перезапуска сервера (ключи клиента сохраняются).

## 🚀 Расширения
- Голос/видео звонки, реактивные presence-индикаторы.
- Плагины-фильтры (скрытие метаданных фото, редактор).
- Key backup с шифрованием на пароль и восстановлением.
