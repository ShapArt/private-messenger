# Desktop client (placeholder)

Planned: Tauri v2 wrapper around web client, system tray, auto-update, secure key vault (OS keychain), file picker with client-side encryption before presigned upload.
